# UI 优化任务清单

> 依据 `UI-OPTIMIZATION.md` 拆解，按实施顺序排列（Token 先行，供后续任务复用）。
> 完成一项勾选一项；每批完成后执行 `./Scripts/build.sh` 验证。

## 任务列表

- [x] **T1 · 设计 Token 收拢**（P3 提前）
  新建 `DesignTokens.swift`：卡片圆角、阴影参数、动画时长（fast 0.15s / normal 0.22s）、瀑布流间距等；提供 `reduceMotion` 判断入口。
- [x] **T2 · 媒体库卡片精修**（P0）
  hover 上浮 + 投影 + 元数据条渐显；元数据条换 `NSVisualEffectView` 毛玻璃；圆角 6→10；选中态 accent 光晕 + 0.15s 动画；卡片背景换语义色。
- [x] **T3 · 缩略图加载体验**（P0）
  SF Symbol 占位 → 中性灰底占位；缩略图到达后 0.2s 渐显；失败态灰底居中小图标。
- [x] **T4 · 空状态升级**（P1）
  48pt SF Symbol 插图 + 标题/副标题 + "导入图片"引导按钮；区分"库为空"与"筛选无结果"两种空态。
- [x] **T5 · 全局过渡动画**（P1）
  主窗口和设置分页最终采用原子即时替换；菜单栏忙碌图标旋转动画；侧栏状态文字 fade。
- [x] **T6 · 分页增量插入动画**（P2）
  追加分页从整体 `reloadData` 改为 `insertItems(at:)`，新卡片渐显入场。
- [x] **T7 · 导入进度窗升级**（P2）
  不确定菊花 → 确定型进度条（scanned 驱动）；统计分色显示（失败红色）；完成态绿色对勾图标。
- [x] **T8 · 菜单栏菜单质感**（P2）
  动作菜单项加 SF Symbol 图标；顶部加当前壁纸缩略图预览项。
- [x] **T9 · 设置页分组卡片视觉升级**（P3，仅视觉）
  平铺表单 → 分组圆角卡片风格。
- [x] **T9.1 · 设置草稿与显式保存**（按后续用户反馈调整）
  显示器和自动化控件先更新页面草稿；点击卡片 footer 中的“保存更改”后一次性提交，并对菜单栏等外部入口的新配置做逐字段三方合并。
- [x] **T10 · 构建验证与回归**
  `./Scripts/build.sh` 通过；`./Scripts/test.sh` 通过；深色/浅色模式无硬编码颜色违和。

## 第二批任务（用户反馈追加）

- [x] **T11 · hover 放大动画更慢更大**
  `cardHoverLift` 1.02→1.05；新增 `animationSlow` 0.4s，悬停提升与元数据渐显改用慢节奏。
- [x] **T12 · 去掉工具栏导入按钮**
  媒体库搜索框右侧的导入按钮移除，只保留右上角工具栏导入入口。
- [x] **T13 · 当前壁纸徽章**
  卡片左上角玻璃胶囊“当前壁纸”徽章；按 contentSHA256 匹配 `currentImageByDisplayUUID`，监听设置变更只刷新可见卡片。
- [x] **T14 · 修复切页文字叠影闪跳**
  crossfade 交叉叠加 → 目标页预先准备并原子替换旧页，主窗口与设置分页一致，不保留透明中间态。
- [x] **T15 · 设置页玻璃卡片 + 配置方式卡片化**
  分组卡片 NSBox → `NSVisualEffectView` 玻璃材质；“配置方式”分段控件 → 两张带图标/副标题/对勾的玻璃选项卡片，选中态 accent 描边。
- [x] **T16 · 状态提示改玻璃 toast**
  新建 `ToastPresenter`：底部居中玻璃胶囊，自动淡出；替换左下角小字状态栏，首页加载/到底/失败时提示。
- [x] **T17 · 双击进入预览页**
  新建 `ImagePreviewOverlayView`：全屏玻璃预览层，大图降采样加载；悬停浮现标题/介绍/详情条；底部横向胶片条点击切换；方向键翻页，Esc/按钮退出。
- [x] **T18 · 第二批构建验证与回归**
  `./Scripts/build.sh` 通过；`./Scripts/test.sh` 通过。

## 第三批任务（显示器页面布局反馈）

- [x] **T19 · 显示器设置页重新排版**
  设置内容改为居中的 820pt 最大阅读列，并用原生纵向滚动承载最小窗口；配置方式卡片保持紧凑等高；规则表单居中并统一控件列宽；共享模式隐藏单屏专属控件；保存状态和按钮收进规则卡 footer。
- [x] **T20 · 修复设置页切换残留媒体库空壳**
  首次进入设置时直接构造目标分页；目标内容替换完成后再暂停媒体库并提交侧栏选中状态，移除主内容和设置子页的双层 alpha 动画。

## 第四批任务（多国家今日壁纸）

- [x] **T21 · 同图多国家元数据独立保存**
  媒体记录身份改为 `rootID + relativeMetadataPath`；下载可复用相同图片字节，但每个国家分别归档标题、介绍、路径和索引。
- [x] **T22 · 今日壁纸分页**
  媒体库新增“媒体库 / 今日壁纸”分段页签；今日页按本地日期查询，并支持全部、中国、美国、日本、德国和法国筛选。
- [x] **T23 · v2 索引迁移与历史记录恢复**
  SQLite 事务升级会校验真实唯一约束、迁移行数和版本；每个目录仅首次串行扫描应用旁车 JSON，恢复旧索引曾覆盖的国家记录。
- [x] **T24 · 异常恢复保护与回归测试**
  损坏待索引队列保留原文件并明确报错；应用旁车读取失败时不标记恢复完成；同图多国家、今日查询和迁移异常均有 XCTest 覆盖。
- [x] **T25 · 关闭主窗口后切换菜单栏模式**
  红色关闭按钮释放主窗口资源后将应用切换为 `.accessory` 并隐藏 Dock；从菜单栏重新打开媒体库或设置时恢复 `.regular` 前台身份。

## 约束

- 纯系统 API，不引入第三方依赖（Swift 6 / AppKit / macOS 13+）
- 所有动画尊重 `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion`
- 卡片复用（`prepareForReuse`）必须重置 hover/动画状态，避免滚动错乱
